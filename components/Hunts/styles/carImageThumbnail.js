import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    height: 130,
    width: 130,
    backgroundColor: 'red',
    marginRight: 7,
  },
  image: {
    height: '100%',
    width: '100%',
  },
})

export default styles
