import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    display: 'flex',
  },
  modelSelector: {
    marginVertical: 10,
    zIndex: 2,
  },
  trimSelector: {
    marginVertical: 10,
    zIndex: 1,
  },
})

export default styles
