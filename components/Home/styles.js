import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  huntsContainer: {
    display: 'flex',
    height: '100%',
  },
})

export default styles
